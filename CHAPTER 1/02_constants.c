#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   int a = 7; //in variable name not allowed with , or space only underscore is allowed & case sensitive
   char harry2 ='c';
   float harry3 = 5.98;
   
   
   
   
   
   printf("Hello, World!");
   return 0;
}