#include<stdio.h>
#include<math.h> // For the pow() function

int main(){
    int a = pow(3, 3);  // This calculates 3 raised to the power of 3
    printf("%d", a);    // %d is the format specifier for printing integers
    return 0;
}
